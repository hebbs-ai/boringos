// SPDX-License-Identifier: MIT
//
// Slack HTTP client. The v1 `ConnectorDefinition` wrapper was
// deleted with the connector framework — `SlackClient` is
// imported directly by the v2 `slack` Module in
// `@boringos/core/src/v2-modules/`.

export { SlackClient } from "./client.js";
